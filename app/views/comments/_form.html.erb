<%= form_with model: [ @location, @location.comments.build ] do |form| %>
    <p>
      <%= form.label :comments %><br>
      <%= form.text_area :body %>
    </p>
    <p>
      <%= form.submit %>
    </p>
  <% end %>