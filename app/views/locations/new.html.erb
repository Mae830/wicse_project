<%= stylesheet_link_tag "my_style" %>
<div class="container">
    <div class="row">
        <div class="col-sm"> 
        </div>

            <div class="col-sm-6 border card">
                <h2>Know a good spot? Tell us about it!</h2>

                <%= render "form", location: @location, button_text: "Add Location" %>
            </div>

        <div class="col-sm">
        </div>

    </div>
</div>